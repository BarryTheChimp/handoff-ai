# Handoff AI - Production Environment Variables
# Copy to .env.production and fill in values before running docker-compose

# ===========================================
# REQUIRED - Docker will fail without these
# ===========================================

# Database credentials (used by both db and backend services)
POSTGRES_USER=handoff
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=handoff

# Security
JWT_SECRET=generate-a-secure-random-string-at-least-32-characters

# AI - Get from https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-api03-...

# ===========================================
# OPTIONAL
# ===========================================

# Frontend port (default: 80)
FRONTEND_PORT=80

# Jira Integration (if using Jira export)
JIRA_CLIENT_ID=
JIRA_CLIENT_SECRET=
